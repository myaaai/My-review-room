<div class="container px-5 px-sm-0">
 <div class="row">
  <div class="col-4 mr-5">
  <%= render 'post_images/list', post_image: @post_image, user: @user %>
  </div>

<div class="col-7 ml-3">
    <div class="row">

<h3 class="font-weight-bold">Category</h3><br>

<%= form_with model: @category,url: categories_path, local: true do |f| %>
<div class="row"><br>

 <%= f.text_field :name, class: 'mt-3 mb-3 mr-5 ml-3' %>
<%= f.submit '新規投稿', class: 'ml-3 mt-3 mb-3 btn btn-outline-success' %>

<% end %>



<table class="table table-border">
	<thead>
		<tr>
			<th>ALL</th>

		</tr>
	</thead>
	<tbody>
		<% @categories.each do |category| %>
			<tr>
				<td><%= category.name %></td>

				<td><%= link_to edit_category_path(category.id), class: 'btn btn-outline-secondary' do %>
			Edit</td>
				<% end %>
				<td><%= link_to "Show all", category_path(category.id), class: 'btn btn-secondary' %></td>
			</tr>
		<% end %>
	</tbody>


 </table>
 </div>
 </div>
</div>